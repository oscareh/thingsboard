ARG TB_BASE_TAG=4.2.1.1
FROM thingsboard/tb-postgres:${TB_BASE_TAG}

COPY docker/thingsboard.deb /tmp/thingsboard.deb

# Extrae el .deb sin ejecutar scripts postinst (más seguro en Docker builds)
RUN dpkg-deb -x /tmp/thingsboard.deb / && rm -f /tmp/thingsboard.deb
